#include <framebuffer.h>
#include <hthread.h>
#include <stdio.h>

void* reduce_thread( void *arg )
{
    printf( "Starting reduce thread...\n" );
    //framebuffer_create( &dst, width, height );
    //framebuffer_linear( &dst, img );
    //framebuffer_destroy( img );
    //framebuffer_clone( img, &dst );
    return NULL;
}
